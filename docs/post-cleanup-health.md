# Проверка после очистки20.09.2026

Runtime1d467fb7e040, свободно54ГБ, занято30%. stroyka/nginx active; health/DB OK.
Публичные /, /app, /health, текущие main JS/CSS возвращают200 с правильным MIME.
Без авторизации /suppliers и /supplier-offers возвращают401.

Обычный вход поставщика:10разделов при390px, нет HTTP/JS ошибок, сообщений error
или переполнения. Обычный вход директора: Главная, Проекты, Склад, Снабжение,
Сметы, Журнал, Настройки открылись; ошибок JavaScript/alerts нет.
Обнаружены6ответов429 при общей загрузке: master-profiles, company-material-aliases,
supply-deliveries, tools, tool-history, inventory. Отдельные последовательные GET
успешны200 (для aliases с обязательным companyId=1; запрос без параметра правильно
даёт422). Это подтверждённый ранее дефект пакетной загрузки, не устранён этой
проверкой; полнота данных при429 не гарантируется. Требуется отдельное исправление.

Служба stroyka-documents-backup имела ошибку от03:15UTC, до сегодняшней очистки.
После освобождения места существующая служба запущена повторно и завершилась
успешно:324файла/336122206байт, контрольные суммы проверены штатным backupскриптом.
В systemctl --failed после проверки0служб. Код службы не менялся.

Бизнес-команды, новые заявки, платежи и сообщения не создавались. Проверка
охватывает доступность/чтение, а не все возможные операции продукта.
Артефакты: /Users/nikolas/.codex/tmp/post-cleanup-health/ и live-tabs.json в
/Users/nikolas/.codex/tmp/supplier-final-acceptance/.
