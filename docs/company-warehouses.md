# Склады компании

## Задача

Следующая вкладка после инвентаризации — «Склады». Завершить каталог складов: принадлежность компании, понятное создание/редактирование, архив вместо удаления, история изменений и безопасный повтор после обрыва связи. Найденная проблема: старые GET/POST/PUT/DELETE работают с глобальной таблицей без компании, а интерфейс скрывает ошибки сохранения.

Существующая модель остатков — один основной склад компании и запасы объектов. Карточки `warehouses` сейчас не участвуют в приходе/перемещении/инвентаризации, колонок `warehouse_id` в БД нет. Вопрос о нескольких самостоятельных складах задан пользователю отдельно. Защита и завершение каталога не зависят от ответа; переход к отдельным остаткам требует явного решения, не выполняется переименованием карточек.

## Приёмка и границы

1. Только выбранная компания и действующее членство; глобальный режим предлагает выбрать компанию. Старые карточки без владельца скрыты до подтверждённой привязки. На сервере найдена одна такая карточка: ID 1 «Лесной склад», Ставрополь, Лесная; вопрос о владельце отправлен отдельно.
2. Просмотр: директор, заместитель, главный инженер, кладовщик, снабженец, бухгалтер. Создание/редактирование — те же роли кроме бухгалтера; архив/возврат из архива — директор и заместитель. Прораб работает с запасами назначенных объектов через уже выпущенную инвентаризацию.
3. Название обязательно, пробелы нормализуются. Поля строго текстовые, ограниченной длины. Два действующих склада одной компании не могут иметь одинаковое название. Совпадение названий разных компаний допустимо.
4. Изменения сохраняются атомарно с автором, временем, исходной и новой карточкой. Владелец неизменяем; физическое удаление закрыто. Архивирование и восстановление требуют основания. Устаревшая версия даёт конфликт, повтор того же UUID возвращает прежний результат без дубля.
5. Интерфейс сообщает об ошибке, сохраняет введённый текст, блокирует повторные клики, очищает данные при смене компании/пользователя. Архив виден отдельно. История открывается из карточки. Мобильная ширина 320 px.
6. Прежний GET `/warehouses` отдаёт только действующие карточки выбранной компании, глобальный режим — пустой список. Прежние мутации закрыты независимо от флага. Остатки, цены, накладные, платежи, инструмент и ведомости сверки не меняются командами каталога.

## Контракт и структура

Новый модуль `backend/features/company_warehouses`, UI `src/features/company-warehouses`, миграция `0031_company_warehouses`. FastAPI/React/PostgreSQL, без новых зависимостей. Отдельная рабочая копия от последнего выпуска; чужие незавершённые изменения не включаются.

- GET `/warehouses/directory`: `{items,canManage,canArchive,truncated}` с архивными карточками.
- GET `/warehouses/{id}/directory`: `{warehouse,history,canManage,canArchive}`.
- POST `/warehouses/directory`: `{action:'create',requestId,name,city?,address?,notes?,expectedCompanyId?,expectedActorId?}`.
- POST `/warehouses/{id}/directory`: `{action:'update'|'archive'|'restore',requestId,expectedVersion,reason?,name?,city?,address?,notes?}`. Update передаёт все четыре текстовых поля. Ответ команды `{ok,warehouseId,eventId}`.
- Карточка `{id,companyId,name,city,address,notes,archived,version,updatedAt}`. Событие содержит автора, основание и снимки до/после.

В `warehouses` добавляются nullable company_id и name_key, archived, version, updated_at; новая таблица `warehouse_directory_events`. name_key формируется нормализацией пробелов и Python casefold, чтобы уникальность кириллицы не зависела от locale базы; у принадлежащей компании карточки ключ обязателен. Legacy-строки не присваиваются компании миграцией. Общий механизм UUID используется для восстановления отправок; автор блокируется до чтения/изменения. Стиль соответствует соседним модулям: `lock_actor(cur, actor)` → проверка версии → запись → событие → commit; ошибка откатывает всю транзакцию.

## Порядок и проверка

Сначала независимые RED-тесты глобального чтения и старых мутаций; затем безопасная схема/сервер и PostgreSQL-сценарии. После этого UI с сетевым повтором, тесты RTL, сборка и реальный браузер на синтетической БД. Ревью, репетиция на закрытой копии, резервная копия, выпуск и проверка доступности/изоляции без тестовых хозяйственных операций в production.

```
PATH=/usr/local/bin:$PATH /Users/nikolas/.codex/tmp/journal-isolation-release/venv/bin/python scripts/run_supplier_catalog_postgres_tests.py backend.features.company_warehouses.test_postgres
CI=true npm test -- --watchAll=false --runInBand
CI=true npm run build
```

Тесты должны доказывать изоляцию, отзыв членства, отсутствие потерянных правок, атомарность, неизменяемость истории, повторы, архивирование и неизменность складского учёта. Не коммитить секреты/сборки. Владельца прежней карточки изменять только после подтверждения пользователя и точной сверки строки. Счета и оплаты поставщикам остаются другому чату.

## Выпуск 19.09.2026

Опубликован `eeb98bf3948108de684b46ff33b1df9188220806` вместо `c941ee43da5cb208bdd72a8cfb0c1b00abefd5cb`. Схема `0031_company_warehouses`, backend-флаг `WAREHOUSE_DIRECTORY_ENABLED=1`; все 20 прежних флагов сохранены. UI всегда использует новый каталог, старые HTTP-мутации закрыты и при отключённом флаге. Прежние глобальные обработчики в main заменены регистрацией модуля; три тестовых хеша main обновлены после независимой проверки этого точного изменения. Логика warehouse recommendation preview не менялась.

Доказательства:

- Исходный RED: 5 тестов, 7 падений, воспроизведены глобальное раскрытие карточек и изменение/удаление чужого склада. После исправления 21 сценарий PostgreSQL и 5 независимых проверок конкуренции/отзыва доступа прошли на отдельных БД. Проверено, что команды каталога не меняют остатки, приходы, оплаты, инструмент и ведомости.
- Итоговый полный backend: 3725 тестов, OK, 604 пропущены по условиям окружения. Из них 26 новых PostgreSQL-сценариев выполнены отдельно (opt-in). Пакет включён в стандартный discover через `__init__.py`.
- Полный React-прогон: 171 набор, 1063 теста. После исправления сброса чужого черновика при recovery — 9 целевых тестов каталога и 28 проверок общих команд/интеграции WarehousePage. Финальная производственная сборка успешна; новых зависимостей нет. Сохраняются прежнее предупреждение Node fs.F_OK и рекомендация сборщика о размере bundle.
- Реальный браузер, синтетический директор и отдельная БД: создание, архив с причиной, восстановление с причиной, история; намеренный обрыв ответа после успешного server commit. Повтор послал тот же UUID и тело, создалась одна карточка с одним событием create. Текст формы пережил сбой, ошибка исчезла после recovery. Итог: version 3, active, ровно create/archive/restore по одному; остаток материала остался 2. На 320 px ширина документа 320 px; скриншот проверен визуально. Недостающая прежняя owner-схема изолированной фикстуры была добавлена до сценария каталога; production-схема эту миграцию уже содержит.
- Независимые reviews сервера, интерфейса и выпуска завершены без блокирующих замечаний. Исправлены casefold-уникальность кириллицы, сброс несвязанного/другого черновика при recovery, отсутствие нового пакета в discover. Сообщение конфликта версии объясняет необходимость закрыть форму и открыть актуальную карточку.
- Репетиция на закрытой копии и production-миграция подтвердили неизменность прежних колонок и записей всех 170 старых таблиц. История каталога после выпуска пуста; старая карточка ID 1 сохранена с company_id/name_key NULL. Проверены хеши 317 файлов.
- Production smoke: API `eeb98bf39481`, база здорова, схема 0031; публичные HTML/JS/CSS соответствуют сборке. Проверены 401 для анонимного доступа к старому и новому каталогу и ранее выпущенным блокам, новых ошибок API нет. В production тестовые карточки и хозяйственные операции не создавались.

Публикация завершена `2026-09-19T07:00:53Z`. Серверный пакет и резервная копия: `/root/stroyka-company-warehouses-3Wo8Mbaj/backup` (БД, старый код, frontend, конфигурация). Закрытые локальные отчёты и сценарии: `~/.codex/tmp/company-warehouses/{deployed,smoke,rehearsal,browser-evidence,cleanup}.json`. Временная БД, синтетические сессии и локальная копия production удалены после проверки.

Откат сохраняет новую схему, владельцев и события. До запуска старого кода `/warehouses` и `/warehouses/` закрываются nginx ответом 503; nginx полностью перезапускается, оба маршрута проверяются, затем возвращается старый runtime. Один graceful reload был отклонён ревью, поскольку старые workers могли бы продолжить обслуживать глобальное чтение. При наличии владельцев/событий downgrade запрещён. Восстановление опубликованного каталога после такого отката требует снятия карантина одновременно с новой защищённой версией.

## Оставшиеся ответы по данным и учёту

Ответа о владельце карточки «Лесной склад», Ставрополь, Лесная пока нет. Она не видна в компаниях, сохранена в БД. Привязка требует подтверждения, сверки исходной строки и отдельного события `assign_owner`; нельзя выбирать владельца по имени склада. Также не получен ответ о нескольких самостоятельных складах. В этом выпуске завершён каталог адресов; основная модель запасов остаётся «один основной склад компании + объекты», самостоятельные остатки каждой карточки не включены.
