# Контроль поставок

Следующий блок после итоговой проверки снабжения. Изменения в существующей
вкладке «Поставки», без новых складских, финансовых или серверных операций.

- Фильтры:все, ожидающие приёмки, принятые без замечаний, с проблемой,
  с незакрытой претензией. Фильтруется весь загруженный список до пагинации.
  При выборе фильтра показ начинается с первых8строк, остальные доступны далее.
- «Открыта» и «В работе» считаются незакрытыми претензиями. Решение претензии
  не отменяет исторического факта проблемной приёмки.
- Принятая поставка определяется статусом «Принято»/«Проблема» либо receivedAt,
  как в серверном обработчике. Кнопка повторной приёмки и прежняя открытая форма
  для неё скрыты. Форма также скрыта у роли без прав приёмки.
- Показываются фактическое принятое количество (включая0), недостача и
  пояснение об исключении брака из доступного остатка. Отсутствующий факт
  количества не подменяется отгруженным количеством.
- Переключение компании/пользователя пересоздаёт панель, сбрасывая её фильтр
  и раскрытие списка. Серверная авторизация остаётся обязательной.

RED:4теста воспроизвели повторную приёмку, потерю претензий «В работе», отсутствие
фильтра старой поставки и доступность устаревшей формы после смены роли.
После исправления targeted7/7 (новые4+существующая пагинация3).
Остатки, документы, сроки поставок, счета/оплаты и подтверждение владельца
старых карточек этим блоком не меняются.


Проверка перед выпуском:175frontend suites/1097tests GREEN, production build
успешен. Независимая ревизия:Required/Critical нет,15смежных тестов GREEN.
Browser на синтетической БД:из11поставок старый pending найден фильтром сразу;
проблемная приёмка показывает факт1/недостача1 и не предлагает повтор;
фильтр незакрытых претензий сохраняет эту поставку.320px:scrollWidth320.
В минимальной fixture известный /suppliers503 и предупреждение preload фонового
изображения; новых ошибок панели поставок нет. Бизнес-операции в browser QA
не отправлялись. Backend runtime и схема не меняются; новых флагов нет.


Выпущено2026-09-19T12:38:52Z:63fe4678265fe56ea66d452052962c9c6ba21a81.
Схема остаётся0033_supply_claim_cases; env/флаги сохранены. Backup:
/root/stroyka-delivery-control-ljAUzgE7/backup.173таблицы сохранены,
319файловfrontend проверены. После выпуска health/db OK, публичные assets
совпали с manifest, защищённые маршруты401 без входа, runtimeErrors0.
Синтетическая БД и браузерные сессии удалены. Старые warehouse/template
ownership остаются открытым пунктом, не блокирующим этот выпуск.
