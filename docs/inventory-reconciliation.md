# Инвентаризация и расхождения

## Задача и порядок

Следующий раздел склада после инструмента — инвентаризация. Пользователь поручил продолжить блоки по порядку. Сверка должна сохранять состояние учёта на момент начала, фактический пересчёт и решение по расхождениям. Старый экран отправляет новую строку при каждом blur, доверяет клиентскому остатку/разнице и не загружает сохранённый факт при открытии.

Принятый порядок: кладовщик/прораб проводит пересчёт, директор или заместитель утверждает корректировку с причиной. Отдельного ответа на необязательный вопрос о роли утверждения не поступило; применён объявленный пользователю вариант с решением директора. Недостача сама по себе не создаёт денежного штрафа. Инструмент по-прежнему меняет состояние через уже выпущенные операции ответственности.

## Приёмочные сценарии

1. Одна выбранная компания; основной склад или точный ID объекта. Сервер фиксирует полный снимок материалов и инструмента. Для неоднозначных названий объектов сверка блокируется; чужие компании и не назначенные прорабу объекты недоступны.
2. «Черновик» → «На проверке» → «Утверждена»; возврат на пересчёт и отмена с причиной. Каждый переход сохраняет неизменяемое событие, автора и дату. Утверждённая ведомость не редактируется и не удаляется.
3. Факт изначально пустой, ноль вводится явно. Повторное сохранение обновляет конкретную строку по ID без дублей; учёт, единица, название, получатель и разница вычисляются сервером. Для расхождения обязательна причина. Сохраняются все позиции снимка, в том числе нулевые остатки.
4. При утверждении сервер повторно проверяет остатки и версии инструмента. Изменившаяся база требует новой сверки; старая остаётся в истории. Корректировка материалов и её история атомарны, остаток не отрицателен. Повтор исходного UUID не проводит разницу дважды.
5. Недостача основного склада требует явного распределения по доступным партиям и/или части без привязки к партии. Излишек учитывается отдельной корректировкой без выдуманной накладной. Исходные приходы, цены, оплаты и договоры не переписываются. На объекте корректируется точная строка складского остатка; материал на руках и в пути не включается в складское наличие.
6. По инструменту сверяется наличие/состояние; утрата или повреждение отражаются в ведомости и направляют к «Операциям и ответственности». Сверка не подменяет выдачу, возврат, назначение ответственного и решение о денежном возмещении.
7. Сохранённая ведомость открывается с теми же учётными и фактическими данными. Печатный акт отражает снимок, расхождения, решение, автора, дату и статус; все тексты экранируются. Интерфейс помещается на телефоне, сетевой повтор сохраняет UUID и содержимое, смена компании/пользователя очищает частные данные.

## Реализация

React/FastAPI/PostgreSQL без новых зависимостей. Модуль backend/features/inventory_reconciliation и src/features/inventory-reconciliation, существующая вкладка «Инвентаризация». Additive migration 0030 после инструмента; старые inventory/inventory_items остаются историей без автоматического переноса остатков. Новые команды за INVENTORY_RECONCILIATION_ENABLED / REACT_APP_INVENTORY_RECONCILIATION_ENABLED; защита уже проведённых сверок сохраняется при отключении.

Сначала failing tests старого поведения и новых команд, затем схема/сервер, интерфейс, PostgreSQL и браузер. Использовать существующие lock_actor, общий stock lock и work_material_operations для повторов. Денежные поля и количество — Decimal, ID строго числовые, клиентские имена/роли не являются доказательством доступа.

API: GET/POST `/inventory/reconciliation` — список с доступными объектами/создание снимка (`projectId`: точный ID либо null для основного склада, `notes`). GET/POST `/inventory/{id}/reconciliation` — чтение и команды (`action`: save/submit/return/cancel/approve, `expectedState`, `requestId`). Save принимает `counts`: список `{key, actual, reason}` для материала или `{key, condition, reason}` для инструмента (`as_recorded/missing/damaged/found`). Учёт и разницу клиент не передаёт. Approve принимает обязательную причину и `lotDeductions`: `{key, untrackedQuantity, lots:[{lotId,quantity}]}` для каждой недостачи основного склада. Все суммы распределения должны точно совпасть с недостачей.

Хранение: прежняя `inventory` служит идентификатором и краткой карточкой; новый `inventory_reconciliations` хранит исходный снимок, текущий факт, версию и состояние. `inventory_reconciliation_events` хранит неизменяемые снимки каждого действия; `inventory_stock_adjustments` — точные изменения материалов с источниками и историей. Старые POST/PUT/DELETE не меняют новые ведомости; при включённом флаге прежние ведомости доступны только для чтения.

Команды проверки:
```
PATH=/usr/local/bin:$PATH /Users/nikolas/.codex/tmp/journal-isolation-release/venv/bin/python scripts/run_supplier_catalog_postgres_tests.py backend.features.inventory_reconciliation.test_postgres
/Users/nikolas/.codex/tmp/journal-isolation-release/venv/bin/python -m unittest discover -s backend -t . -p 'test_*.py'
CI=true npm test -- --watchAll=false --runInBand
CI=true npm run build
```

## Границы

Счета и оплаты поставщикам остаются другому чату. Данные production сначала проверяются чтением; хозяйственные операции при тестировании выполняются только в изолированной БД. Перед выпуском — репетиция миграции, резервная копия и проверка неизменности прежних данных. Не приписывать старые расхождения людям, не удалять старые ведомости и не включать недостачу в штраф автоматически.

## Выпуск 19.09.2026

Опубликован `c941ee43da5cb208bdd72a8cfb0c1b00abefd5cb` вместо `cecfcb9fc0261dc758d7eddc9ad6a4006ae74207`, миграция `0030_inventory_reconciliation`. Backend/frontend флаги инвентаризации включены; флаги прежних блоков сохранены. Прорабу добавлен вход «Склад» только в инвентаризацию назначенных объектов. Остальные складские вкладки этому входу не предоставляются.

Проверки:

- 34 сценария на отдельных PostgreSQL-базах: 15 жизненного цикла, 9 старых маршрутов, 6 границ доступа/учёта, 4 независимого ревью; отдельно 5 тестов политики. Проверены партии и остаток без партии, неизменность договоров/накладных, атомарный откат, повторы после последующих сверок, смена членства, неоднозначные объекты, защищённые события и отказ downgrade при наличии данных.
- Полный backend-прогон: 3689 тестов, OK, 568 пропущены по условиям окружения; указанные PostgreSQL-проверки выполнены отдельно. Полный React-прогон: 168 наборов, 1055 тестов. После последней правки доступа прораба — 3 целевых набора, 11 тестов. Производственная сборка успешна.
- Реальный браузер с синтетическими аккаунтами и отдельной БД: прораб создаёт ведомость своего объекта, сохраняет факт и передаёт директору; директор утверждает. Ответ первого утверждения намеренно оборван после сохранения на сервере. Повтор сохранённой отправки завершился без ошибки и без второй корректировки: остаток 2 → 1,25, ровно одно событие approve и одна корректировка. На ширине 320 px ширина документа также 320 px. Печатный предпросмотр содержит сохранённый факт, расхождение, автора и решение директора; проверен визуально.
- Независимые ревью сервера, интерфейса и выпуска завершены без блокирующих замечаний. Исправлены обнаруженные проблемы нулевого ID, триггера неизменяемости и устаревшей ошибки после успешного сетевого повтора.
- Репетиция на закрытой копии базы и реальная миграция подтвердили неизменность прежних колонок и записей всех 167 старых таблиц. Добавлены 3 пустые таблицы, без хозяйственных операций и переноса старых остатков. Проверены хеши 317 опубликованных файлов.
- Production smoke: версия API `c941ee43da5c`, база здорова, схема 0030; публичные HTML/JS/CSS соответствуют сборке. Проверены 401 для запросов без входа к инвентаризации и ранее выпущенным блокам; новых ошибок API не обнаружено. Авторизованные хозяйственные сценарии проверены в изолированной БД, в production тестовых списаний не делалось.

Серверный выпуск и резервная копия: `/root/stroyka-inventory-2WBtquiL/backup` (база, прежний код, frontend, конфигурация). Публикация завершена `2026-09-19T06:41:19Z`. Закрытые локальные отчёты: `~/.codex/tmp/inventory-reconciliation/{deployed,smoke,rehearsal,browser-evidence}.json`, журналы проверок и сценарий восстановления. Если появились события новых ведомостей, восстановление сохраняет новый код и защитные ограничения, отключая новые команды флагом; откат схемы с такими данными запрещён.

Проверка существующих данных показала один объект с неоднозначным названием (ID 24): создание сверки для него блокируется до отдельного устранения неоднозначности. Автоматической привязки старых остатков к нему не выполнялось. Ранее отмеченные карточки инструмента без точной привязки также не переатрибутированы.
