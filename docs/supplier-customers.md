# P8 — заказчики поставщика

Список строится из доступных адресных requests/offers, группировка по companyId,
никогда по названию. Общий каталог компаний не запрашивается. История включает
заявки, выбранные КП как заказы и документы с проверенной связью company/request/
offer/supplier. Контактные данные и реквизиты не расширяются относительно API.

Один полный снимок requests/offers/deliveries/invoices; загрузка и ошибка скрывают
прежние данные. Фильтр Все заказчики и карточка компании используют один снимок.
После отзыва доступа карточка исчезает, старое выбранное значение не раскрывает
данные. Переходы ведут к исходной заявке и существующим действиям. Финансовые
операции не меняются. Проверки: одинаковые названия разных компаний, чужие
документы, 2 заказчика, отзыв/ошибка, другой поставщик, mobile320, выпуск без миграции.

## Проверки перед выпуском

193 frontend suites / 1184 tests PASS; production build PASS.
Реальный браузер на локальном PostgreSQL: компании №2/№3 (13/12 заявок),
заказ и поставка только у №2; переход в заявку №25; 503 скрывает историю,
повторная загрузка восстанавливает её. Другой поставщик: 0 заказчиков.
Mobile 320px: ширина документа 320px, горизонтального переполнения нет.
Проверен общий компонент заказов после извлечения; links разрешают только HTTP(S).
Серверные права и финансовые операции не менялись, миграций нет.
Откат: предыдущий код d7406367 и сохранённая сборка, база не откатывается.

## Выпуск

Production 979ef693, 2026-09-20 00:45 МСК (2026-09-19T21:45:29Z).
Резервная копия: `/root/stroyka-customers-RJT2rDFO/backup`.
Данные всех 173 таблиц сохранены; проверены 320 файлов сборки.
Схема прежняя: `0034_supplier_response_due`. Публичные JS/CSS соответствуют сборке,
защищённые маршруты без сессии возвращают 401. Health OK, DB 12.6ms,
новых ошибок API при проверке выпуска нет.

Preflight обнаружил старое ограничение health: `_app_version` читает только loose
Git refs, после автоматического GC возвращает unknown. Восстановлен тот же main ref
как отдельный файл, без изменения HEAD; автоматический GC отключён для release fetch.
Повторный preflight и post-release health прошли. Поддержка packed/common refs
зафиксирована отдельным техническим обязательством, не считается исправленной в коде.
