# Исправление автозаполнения журналов — 18.09.2026

Пользователь выбрал исправление двух ошибок: входного контроля материалов и
кабельного журнала. База отдельного выпуска — production 88677729, после
завершённого каталога поставщиков. Счета и оплаты продолжаются в другом чате.

## Границы

- Исправить передачу LIKE-шаблона «приход%» в psycopg2: сейчас процент внутри
  SQL воспринимается как форматный маркер при передаче списка параметров.
- Сохранить фильтрацию по проекту, знаку количества и виду движения.
- Проверить повторный запуск и устранить обнаруженную в готовом исправлении
  связанную проблему единиц с пробелами, чтобы восстановленный путь не плодил
  записи. Сохранённые единицы и количества не нормализовать заново.
- Переиспользовать проверенные изменения 4da325de и 7fcc47b7 из будущей ветки,
  без выпуска её функций распределения склада, финансов и владельцев журналов.
- Новых миграций, feature flags, модели/AI и внешних отправок не требуется.

## Проверки и выпуск

- [x] Воспроизвести ошибку на отдельном PostgreSQL для обоих журналов с фильтром
  проекта и без него; воспроизвести повторные записи нестандартных единиц.
- [x] Применить минимальные исправления и получить зелёные шесть регрессий.
- [x] Проверить реальные данные на закрытой локальной копии только нужных
  таблиц: прежние записи, склад и первичные документы неизменны, повтор идемпотентен.
- [x] Проверить backend и совместимость поставщиков; рассмотреть точный diff.
- [x] Сохранить текущую БД/код, выпустить только исправление backend, проверить
  обе страницы в существующей авторизованной браузерной сессии и журнал сервиса.
- [x] Записать версию, результаты и путь отката.

Копия production: 340 записей входного контроля неизменны; кабельный журнал
18 → 19, повтор 0/0. Все прежние строки, 52 накладные, 515 складских движений
и 298 остатков сохранены побайтно в представлении строк PostgreSQL.
23 PostgreSQL-теста прошли (6 журналов + 17 совместимости каталога поставщиков).
Общий backend: 3079 тестов, OK, 90 opt-in пропусков. Запуск — подготовленное
окружение `/tmp/stroyka-catalog-venv/bin/python`, стандартная команда CI
`-m unittest discover -s backend -p 'test_*.py' -t .`. Первый запуск системным
Python выявил только отсутствие reportlab; код для обхода этого не менялся.

Выпущено `25374a3360c2` в 20:49:15 МСК. Бэкап:
`/root/stroyka-journal-backfill-aVpuiZAa/backup`. Обе страницы проверены после
перезагрузки через рабочий вход: 340 и 19 записей, повтор без изменений,
ошибок JavaScript при переключении журналов нет. Старых backfill-ошибок,
IndexError и Traceback в журнале сервиса после выпуска нет.

Отдельное наблюдение: чтение входного контроля занимает 9,65–11,57 с;
оптимизация синхронного legacy-backfill остаётся отдельной задачей.
